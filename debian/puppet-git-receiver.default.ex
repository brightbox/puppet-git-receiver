# Defaults for puppet-git-receiver initscript
# sourced by /etc/init.d/puppet-git-receiver
# installed at /etc/default/puppet-git-receiver by the maintainer scripts

#
# This is a POSIX shell fragment
#

# Additional options that are passed to the Daemon.
DAEMON_OPTS=""
